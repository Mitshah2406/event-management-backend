# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=evently_user
DB_PASSWORD=evently_password
DB_NAME=evently_db
DB_SSLMODE=disable

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Server Configuration
PORT=8080
GIN_MODE=debug
API_VERSION=v1
API_PREFIX=/api

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=86400        # 24 hours
JWT_REFRESH_EXPIRES_IN=86400  # 24 hours = 86400 seconds


# RATE LIMITING CONFIGURATION
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_DURATION=60s                # 1 minute window

# Standard endpoints
RATE_LIMIT_DEFAULT_REQUESTS=200               # General endpoints
RATE_LIMIT_PUBLIC_REQUESTS=500                # Public browsing (events, tags, sections)
RATE_LIMIT_USER_REQUESTS=300                  # User-specific operations

# Authentication endpoints (prevent brute force)
RATE_LIMIT_AUTH_REQUESTS=60                   # Login/register/refresh

# Booking flow (CRITICAL for business)
RATE_LIMIT_BOOKING_REQUESTS=200                # General booking operations
RATE_LIMIT_BOOKING_CRITICAL_REQUESTS=50       # Payment/confirm operations (stricter)

# Administrative endpoints
RATE_LIMIT_ADMIN_REQUESTS=1200                 # Admin operations (high limit)
RATE_LIMIT_ANALYTICS_REQUESTS=120             # Analytics dashboards

# Health/monitoring endpoints
RATE_LIMIT_HEALTH_REQUESTS=1000               # Health checks (very high limit)

# Whitelisted IPs (comma-separated, no spaces)
RATE_LIMIT_WHITELISTED_IPS=127.0.0.1,::1      # Localhost IPv4 and IPv6



# External Services (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=elitecoders89@gmail.com
SMTP_PASSWORD=khoydlnfiirzftne
FROM_EMAIL=elitecoders89@gmail.com